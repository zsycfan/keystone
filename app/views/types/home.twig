This is the homepage.
<hr />

{% for element in page.tagline %}
	<h1>{{ element.publishedRevision().getFieldId(1).value }}</h1>
{% endfor %}

{% for element in page.body %}
	<p>{{ element.publishedRevision().getFieldId(1).value }}</p>
{% endfor %}